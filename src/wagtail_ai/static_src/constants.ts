export enum DefaultPrompt {
  CORRECTION = 1,
  COMPLETION = 2,
  DESCRIPTION = 3,
  TITLE = 4,
  CONTEXTUAL_ALT_TEXT = 5,
  IMAGE_TITLE = 6,
  IMAGE_DESCRIPTION = 7,
}

export enum PromptMethod {
  APPEND = 'append',
  REPLACE = 'replace',
}
